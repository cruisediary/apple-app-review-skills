Run !`echo "${CLAUDE_CONFIG_DIR:-$HOME/.claude}"` to resolve the Claude config directory.

Then read the file `agents/appstore-full-audit.md` inside that directory and execute all instructions in it against the current project.
